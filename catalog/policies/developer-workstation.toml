schema_version = "1.0"
id = "developer-workstation"
allow_packages = []
deny_packages = []
allow_categories = ["developer-tools", "editors", "runtimes", "containers", "databases", "terminals", "system-utilities"]
deny_categories = []
allow_sources = []
deny_sources = []
allow_backends = ["winget", "homebrew-formula", "homebrew-cask", "apt", "dnf", "yum", "pacman", "zypper", "apk", "flatpak"]
deny_backends = ["artifact"]
allow_channels = ["stable", "beta"]
deny_channels = ["nightly", "custom"]
allow_scopes = ["user", "system", "auto"]
deny_scopes = []
allow_licenses = []
deny_licenses = []
preferred_backends = ["homebrew-formula", "winget", "apt", "dnf", "yum", "pacman"]
require_signatures = true
require_digests = true
trusted_publishers = []
minimum_provenance = "backend-repository"
forbid_artifacts = true
forbid_prerelease = false
require_confirmation = true
require_dry_run = false
network_domains = []
prevent_downgrade = true
prevent_uninstall = false
freshness_days = 30
